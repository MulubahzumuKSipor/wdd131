const year = document.querySelector("#year");
year.innerHTML = `getFullYear(): <span class="highlight">${today. new Date(document.lastModified)}</span>`;
let oLastModif = new Date(document.lastModified);